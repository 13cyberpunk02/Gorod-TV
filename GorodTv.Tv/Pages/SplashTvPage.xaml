<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="GorodTv.Tv.Pages.SplashTvPage"
             Shell.NavBarIsVisible="False"
             BackgroundColor="#0B0E13">
    <Grid>
        <VerticalStackLayout HorizontalOptions="Center" VerticalOptions="Center" Spacing="20">
            <Border BackgroundColor="{StaticResource Primary}" StrokeThickness="0"
                    StrokeShape="RoundRectangle 22" WidthRequest="96" HeightRequest="96"
                    HorizontalOptions="Center">
                <Image Source="appicon.svg" Aspect="AspectFit" Margin="16" />
            </Border>
            <Label Text="Инфо-Лан ТВ" FontFamily="OnestBold" FontSize="24"
                   TextColor="White" HorizontalOptions="Center" />
            <ActivityIndicator IsRunning="True" Color="{StaticResource Primary}"
                               HorizontalOptions="Center" />
        </VerticalStackLayout>
    </Grid>
</ContentPage>